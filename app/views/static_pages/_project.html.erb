<li>
  <%= link_to project.title, root_path %>
    <ul class="unstyled">
    <% project.users.each do |u| %>
    <li><%= gravatar_for(u, :size => 20) %> <%= u.name %></li>
    <% end %>
  </ul>
  <br />
  <%= link_to "Edit", edit_project_path(project), :class => 'btn btn-mini' %>
</li>